import java.util.*;
public class charusat {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter the string that you want to enter: ");
        String herish = new String();
        if(Character.isWhitespace(herish) ){
            herish.replace('a','z');
        }
        // if(Character.isWhitespace(herish) == 1){

        //     String[] parts = herish.split(" ", 2);
        //     if (parts.length > 1) {

        //         parts[1]= "CHARUSAT";
        //         // Print the second part of the string
        //         System.out.println(parts[1]);
        //     } else {
        //         // Handle case where there is no whitespace
        //         System.out.println("No whitespace found in the string.");
        //     }

        // }
        

    }
}
